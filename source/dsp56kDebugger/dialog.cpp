#include "dialog.h"

namespace dsp56kDebugger
{
	Dialog::Dialog(wxWindow* _parent, const std::string& _title, const wxPoint& _pos, const wxSize& _size) : wxDialog(_parent, wxID_ANY, _title, _pos, _size)
	{
	}
}
